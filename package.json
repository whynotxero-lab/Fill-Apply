{
  "name": "fill-and-apply",
  "version": "1.15.0",
  "private": true,
  "description": "Chrome MV3 queue-driven job-application filler. The extension itself has no build step; this manifest only wires up the test harness.",
  "scripts": {
    "test": "node scripts/run-tests.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}
